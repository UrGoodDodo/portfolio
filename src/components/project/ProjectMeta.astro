---
import type { Language } from '../../i18n/config';
import { getUi } from '../../i18n/utils';

import '../../styles/project/project-meta.css';

interface Props {
  lang: Language;
  year: number;
  roles: string[];
  technologies: string[];
}

const {
  lang,
  year,
  roles,
  technologies,
} = Astro.props;

const t = getUi(lang);
---

<div class="project-meta">

  <div class="project-meta__item">
    <span class="project-meta__label">
      {t.project.role}
    </span>

    <span class="project-meta__value">
      {roles.join(' · ')}
    </span>
  </div>

  <div class="project-meta__item">
    <span class="project-meta__label">
      {t.project.technologies}
    </span>

    <span class="project-meta__value">
      {technologies.join(' · ')}
    </span>
  </div>

  <div class="project-meta__item">
    <span class="project-meta__label">
      {t.project.year}
    </span>

    <span class="project-meta__value">
      {year}
    </span>
  </div>

</div>